inherits:
  - ../d13a_edgeaware_lite_localpool_k144.yaml

run:
  config_name: d13a_edgeaware_lite_localpool_k144_ep100

optimizer:
  lr: 0.0001

training:
  epochs: 100
  max_epochs: 100
  early_stopping_metric: val_macro_f1
  early_stopping_patience: 25
  resume_optimizer_state: false
  resume_scheduler_state: false
  amp: false
