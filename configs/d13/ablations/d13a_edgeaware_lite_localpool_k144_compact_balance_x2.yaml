inherits:
  - ../d13a_edgeaware_lite_localpool_k144.yaml

run:
  config_name: d13a_edgeaware_lite_localpool_k144_compact_balance_x2

loss:
  pool_balance_weight: 0.002
  pool_compactness_weight: 0.002
  supcon_weight: 0.0

training:
  epochs: 50
  max_epochs: 50
  early_stopping_metric: val_macro_f1
  amp: false

